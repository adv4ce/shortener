# ✨ Shortener

[![Go](https://img.shields.io/badge/Go-1.21-00ADD8?style=for-the-badge&logo=go)](https://go.dev/)

> Простой проект для укорачивания ссылок.
> 
> Рабочий проект: https://shrtnr.ru/
>
> Телеграм бот: [@shrtnr_bot](https://t.me/shrtnr_bot)
## ✨ Основные функции

*   **Основной сервис укорачивания URL:** Способен принимать длинные URL и генерировать уникальные короткие URL, которые затем можно использовать для перенаправления.
*   **Постоянное хранение URL:** Использует базу данных SQLite (`db.sqlite3`) для эффективного хранения пар исходных URL и сгенерированных коротких URL.
*   **RESTful API:** Предоставляет конечные точки API для программного взаимодействия с сервисом, позволяя другим приложениям сокращать и получать URL.
*   **Валидация URL и генерация уникальных кодов:** Реализует логику для валидации формата введенного URL и генерации уникальных непересекающихся коротких кодов для каждой новой ссылки.
*   **Модульная архитектура Go:** Следует лучшим практикам Go с четким разделением логики базы данных, обработчиков API и бизнес-сервисов, что способствует чистому и легко поддерживаемому коду.

## 🛠️ Технологический стек

| Категория                | Технология | Примечание                                        |
| :----------------------- | :--------- | :------------------------------------------------ |
| **Основной язык**        | Go         | Основной язык программирования для бэкенд-приложения. |
| **База данных**          | SQLite     | Легковесная реляционная база данных для хранения данных URL. |
| **Управление зависимостями** | Go Modules | Официальная система управления зависимостями для Go. |

## 🏛️ Обзор архитектуры

Проект Shortener разработан как простой веб-сервис на Go, следующий принципам чистой и модульной архитектуры. Он использует стандартную структуру проекта Go:

*   **`cmd/`**: Содержит файл `main.go`, который является основной точкой входа для приложения.
*   **`internal/`**: Содержит всю основную логику приложения, которая не предназначена для импорта внешними проектами. Она дополнительно разделена на несколько частей:
    *   **`database/`**: Обрабатывает подключение к базе данных SQLite и все CRUD-операции (Create, Read, Update, Delete), связанные с данными URL.
    *   **`handlers/`**: Определяет маршруты и логику для конечных точек API, обрабатывает входящие запросы и возвращает соответствующие ответы.
    *   **`services/`**: Содержит основную бизнес-логику, такую как валидация URL и создание уникальных коротких кодов.

Это разделение обеспечивает четкое разделение ответственности, делая код более понятным, тестируемым и поддерживаемым.

## 🚀 Начало работы

Следуйте этим шагам, чтобы запустить проект Shortener локально на вашей машине.

### Предварительные требования

Убедитесь, что у вас установлен Go (версии 1.16 или выше) в вашей системе.

### Установка

1.  **Клонирование репозитория:**
    ```bash
    git clone https://github.com/adv4ce/shortener.git
    cd shortener
    ```

2.  **Инициализация модулей Go:**
    Загрузка всех необходимых зависимостей проекта.
    ```bash
    go mod tidy
    ```

3.  **Запуск приложения:**
    Запуск сервера приложения.
    ```bash
    go run cmd/main.go
    ```
    Приложение должно быть запущено на `http://localhost:8080` (или на любом другом настроенном порту).

## 📂 Структура файлов
```
/
├── .gitignore
├── cmd
│   └── main.go
├── db.sqlite3
├── go.mod
├── go.sum
└── internal
    ├── database
    │   ├── connect.go
    │   └── requests.go
    ├── handlers
    │   ├── api.go
    │   └── router.go
    └── services
        ├── check_url.go
        └── code_generator.go
```

## 📄 API 
### Получить короткий код для ссылки:

`/link`

*Тело запроса:*
```json
"url": "ссылка"
```

### Перейти по короткой ссылке

`/:code`

`:code` - короткий код, который возвращается после запроса `/link`
